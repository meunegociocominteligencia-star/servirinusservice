name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # Permite disparar manualmente na aba Actions do GitHub

jobs:
  build:
    name: Compilar APK
    runs-on: ubuntu-latest

    steps:
    - name: Obter Código Fonte (Checkout)
      uses: actions/checkout@v4

    - name: Configurar Java JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Configurar Gradle (Sem Cache)
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.2'
        cache-disabled: true

    - name: Construir Wrapper e Compilar APK
      run: |
        cd android
        # Gerar o jar do wrapper que está ausente no repositório
        gradle wrapper --gradle-version 8.2
        chmod +x gradlew
        ./gradlew assembleDebug --stacktrace

    - name: Disponibilizar APK para Download (Upload Artifact)
      uses: actions/upload-artifact@v4
      with:
        name: severinu-service-app-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
